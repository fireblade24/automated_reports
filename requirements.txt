# No mandatory third-party dependencies for the POC runtime.
# Optional integrations:
# - OpenAI API via HTTPS (no SDK required)
# - BigQuery via `bq` CLI
# - WeasyPrint for enhanced PDF rendering (`pip install weasyprint`)
